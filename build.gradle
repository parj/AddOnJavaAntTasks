apply plugin: 'java'

repositories {
    mavenCentral()
}

List compileAll = ["org.apache.ant:ant:1.8.1",
				   "xmlunit:xmlunit:1.3", "net.sourceforge.jexcelapi:jxl:2.6.10", //org.pm.xml.AntXMLUnit
				   "commons-net:commons-net:1.4.1", "log4j:log4j:1.2.16", "net.sf.opencsv:opencsv:2.0", //org.pm.csv.diff
				   "net.sourceforge.jexcelapi:jxl:2.6.10", //org.pm.csv.AntCsvToExcel
				   "org.apache.jackrabbit:jackrabbit-webdav:2.1.1" //org.pm.AntDav 
				   ]
dependencies {
    compile compileAll
}

allprojects {
    apply plugin: 'java'
    group = 'org.pm'
    version = '1.0'
    
    repositories {
        mavenCentral()
    }
}

/*project(':org.pm.xml.AntXMLUnit') {
    List dependencyXMLUnit = ["xmlunit:xmlunit:1.3", "net.sourceforge.jexcelapi:jxl:2.6.10"]
    dependencies {
        compile compileAll
        compile dependencyXMLUnit
    }
}

project(':org.pm.csv.diff') {
    List dependencyCSVDiff = ["commons-net:commons-net:1.4.1", "log4j:log4j:1.2.16", "net.sf.opencsv:opencsv:2.0"]
    dependencies {
        compile compileAll
        compile dependencyCSVDiff
    }
}

project(':org.pm.csv.AntCsvToExcel') {
    List dependencyAntCsvToExcel = ["net.sourceforge.jexcelapi:jxl:2.6.10"]
    dependencies {
        compile compileAll
        compile dependencyAntCsvToExcel
    }
}

project(':org.pm.AntDav') {
    List dependencyAntDav = ["org.apache.jackrabbit:jackrabbit-webdav:2.1.1"]

    dependencies {
        compile compileAll
        compile dependencyAntDav
    }
}*/